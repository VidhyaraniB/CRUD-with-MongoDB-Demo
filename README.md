This project demonstrates how to build a simple CRUD (Create, Read, Update, Delete) application using Spring Boot and MongoDB.

Prerequisites :-
Java 17+
Maven or Gradle
MongoDB installed locally MongoDB Compass

This demo shows how to:

Connect Spring Boot to MongoDB,
Define a domain model with annotations,
Use Spring Data MongoDB repositories,
Expose CRUD endpoints via REST,
Test with Postman and MongoDB Compass
